<UserControl x:Class="CoffeeShop.CrystalReports.Wpf.Views.CrystalReportDoanhThuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="720"
             d:DesignWidth="1180"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Báo cáo Crystal Report - Doanh thu theo khoảng ngày"
                       FontSize="18"
                       FontWeight="Bold" />
            <TextBlock Foreground="Gray"
                       Text="Chọn khoảng ngày, nhân viên (tùy chọn), danh mục (tùy chọn) rồi bấm 'Xem báo cáo'." />
        </StackPanel>

        <!-- Vùng tham số -->
        <Border Grid.Row="1"
                Padding="10"
                Background="#FFF7F7F7"
                BorderBrush="#FFD9D9D9"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Từ ngày" FontWeight="SemiBold" />
                    <DatePicker x:Name="dpTuNgay" Width="150" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Đến ngày" FontWeight="SemiBold" />
                    <DatePicker x:Name="dpDenNgay" Width="150" />
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="Nhân viên" FontWeight="SemiBold" />
                    <ComboBox x:Name="cboNhanVien"
                              Width="200"
                              DisplayMemberPath="HoTen"
                              SelectedValuePath="NguoiDungId" />
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="0,0,12,0">
                    <TextBlock Text="Danh mục" FontWeight="SemiBold" />
                    <ComboBox x:Name="cboDanhMuc"
                              Width="200"
                              DisplayMemberPath="TenDanhMuc"
                              SelectedValuePath="DanhMucId" />
                </StackPanel>

                <Button x:Name="btnXemBaoCao"
                        Grid.Column="5"
                        Width="140"
                        Height="34"
                        VerticalAlignment="Bottom"
                        Background="#FF1F6FEB"
                        Click="btnXemBaoCao_Click"
                        Content="Xem báo cáo"
                        FontWeight="Bold"
                        Foreground="White" />
            </Grid>
        </Border>

        <!-- Crystal Reports WinForms viewer is hosted here to avoid the SAP WPF viewer WindowInteropHelper issue. -->
        <Border Grid.Row="2"
                Margin="0,10,0,0"
                BorderBrush="#FFD9D9D9"
                BorderThickness="1">
            <Grid x:Name="viewerHost" />
        </Border>
    </Grid>
</UserControl>
